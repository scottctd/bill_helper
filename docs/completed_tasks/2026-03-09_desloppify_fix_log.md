# Desloppify Fix Log

Date: 2026-03-09

This file records durable fix batches completed during the March 9, 2026 desloppify cleanup pass.

| Finding ID | Problem | Concrete Fix | Prevention Rule |
|---|---|---|---|
| `review::.::holistic::abstraction_fitness::agent_runtime_wrapper_chain::87832f3e`, `review::.::holistic::ai_generated_debt::agent_execution_passthrough_layer::e3fd931a` | Agent runtime and benchmark callers depended on wrapper-only forwarding helpers instead of the owning modules. | Removed one-line passthrough helpers from `backend/services/agent/execution.py`, moved production imports to the owning runtime/tool modules, and kept `backend/services/agent/tools.py` as an explicit compatibility re-export surface. | Compatibility files may re-export stable contracts, but production code should import the owner module directly instead of layering wrapper seams. |
| `review::.::holistic::abstraction_fitness::change_pipeline_payload_bags::9f5d37fb` | The agent review/apply path repeatedly reparsed loose dict payload bags instead of sharing one typed change contract. | Added canonical typed payload parsing in `backend/services/agent/change_contracts.py` and threaded typed payload objects through `review.py`, `change_apply.py`, entry/group reference helpers, and proposal handlers. | Parse and validate change payloads once at the boundary, then pass typed objects through the rest of the pipeline. |
| `review::.::holistic::contract_coherence::stringly_typed_tool_result_status::ef3bf11d` | Tool execution results encoded status with raw strings, so runtime, handlers, and benchmark traces could drift. | Introduced `ToolExecutionStatus` in `backend/services/agent/tool_types.py` and updated runtime, handlers, compatibility exports, and benchmark traces to use the enum consistently. | Shared cross-module status/result values should come from one typed enum, not repeated string literals. |
| `review::.::holistic::convention_outlier::agent_normalization_helpers_split_across_siblings::bec181cb` | Agent payload normalization helpers were duplicated across sibling modules. | Extracted shared text/category normalization helpers into `backend/services/agent/payload_normalization.py` and updated callers to use the canonical module. | Common normalization rules belong in one shared helper module so validation semantics cannot drift across siblings. |
| `review::.::holistic::api_surface_coherence::account_update_parameter_sprawl::11505d20` | `update_account_root(...)` exposed partial-update behavior through a sentinel-heavy multi-parameter API. | Added a service-owned `AccountPatch` model in `backend/services/accounts.py`, switched router and agent update flows to build that typed patch, and added regression coverage for clearing account notes through both paths. | Partial mutations should travel through one explicit patch contract shared by every caller instead of public sentinel arguments. |
| `review::.::holistic::api_surface_coherence::service_error_contract_drift::9665492c` | Account payload services translated CRUD failures directly into HTTP exceptions while sibling finance services used a shared policy-error contract. | Switched `backend/services/accounts.py` back to `PolicyViolation` for account create/update validation and let `backend/routers/accounts.py` own the HTTP translation, while agent apply handlers normalize the same policy errors into proposal-friendly `ValueError` messages. | Service modules should expose one shared domain/policy error contract and leave HTTP status translation at router boundaries. |
| `review::.::holistic::contract_coherence::admin_policy_literal_drift::10fee856` | Admin-only behavior depended on repeated raw `"admin"` string checks spread across auth, access-scope, routers, and agent readers. | Promoted canonical admin helpers to `backend/auth.py` and updated access scoping, user routes, and agent read tools to reuse that one source of truth. | Permission rules should flow from one canonical helper or constant, not repeated string literals across modules. |
| `review::.::holistic::contract_coherence::currencies_route_owner_scope_gap::35eb587d` | `/currencies` counted every entry in the database instead of following the principal-aware owner scope used by sibling finance endpoints. | Added request-principal dependency and `entry_owner_filter(...)` scoping to `backend/routers/currencies.py`, plus a regression test that non-admin users only see their own non-default currency counts. | Finance read endpoints must apply the same owner-scope helpers as sibling routes so aggregate data does not leak across principals. |
| `review::.::holistic::contract_coherence::json_helper_allows_empty_model_payloads::e4985269` | The Telegram API client treated a successful empty response body as `None` even though every current caller immediately validates it as a concrete model payload. | Tightened `telegram/bill_helper_api.py` so `_read_json_response()` raises `BillHelperApiError` on empty or invalid JSON success bodies, and added `telegram/tests/test_bill_helper_api.py` coverage for the fail-closed behavior. | Transport helpers should expose typed success contracts that either return valid JSON payloads or raise immediately; they should not leak sentinel `None` values into model-bound callers. |
| `security::backend/services/agent/model_client.py::security::hardcoded_secret_name::backend/services/agent/model_client.py::44`, `security::telegram/webhook.py::security::hardcoded_secret_name::telegram/webhook.py::16` | Detector-facing constant names made configuration/header labels look like embedded secrets even though they only named environment and header keys. | Renamed the constants to auth/header terminology (`_BEDROCK_AUTH_ENV_NAME`, `TELEGRAM_WEBHOOK_HEADER_NAME`) while preserving the underlying env var and HTTP header values and keeping webhook/model-client behavior unchanged. | Constants that identify config keys or header names should describe their role, not imply that the constant itself stores secret material. |
| `review::.::holistic::design_coherence::entries_router_owns_domain_workflow::f2b899d0` | Entry create/update handlers still performed ownership checks, entity/user resolution, tag mutation, and direct-group persistence inside the router layer. | Added service-owned `EntryCreateCommand` and `EntryUpdateCommand` workflows in `backend/services/entries.py`, promoted principal-scoped loaders in `backend/services/access_scope.py`, slimmed `backend/routers/entries.py` back to HTTP translation, and added direct service tests that cover grouped entry creation plus policy-contract failures. | Routers should translate HTTP only; multi-step entry mutation workflows belong behind typed service commands with direct service-level regression tests. |
| `review::.::holistic::convention_outlier::legacy_backend_facades_obscure_split_modules::2ebcd0f1`, `review::.::holistic::cross_module_architecture::legacy_facades_still_shape_architecture::7bf673dc` | Thin `backend/models.py`, `backend/schemas.py`, and `backend/enums.py` facades still anchored docs and tests to the legacy aggregate surface even though production code had already moved to split domain modules. | Deleted the three facade modules, updated the remaining test import to use `backend.models_agent` and `backend.enums_agent` directly, added a guard in `backend/tests/test_import_conventions.py` to keep the facades deleted, and rewrote stable docs to reference the real `*_finance` and `*_agent` modules. | Once a domain split is complete, remove aggregate compatibility facades and point docs/tests at the owning modules so old architecture landmarks cannot re-form. |
| `review::.::holistic::cross_module_architecture::router_service_boundary_drift::a0f9a33b` | Tags, taxonomies, users, and dashboard routers still assembled persistence queries and read-model orchestration inline instead of delegating those workflows to services. | Moved tag read-model assembly into `backend/services/tags.py`, taxonomy read/write orchestration into `backend/services/taxonomy.py`, user list/update read models into `backend/services/users.py`, and dashboard read assembly into `backend/services/finance.py`, leaving the routers to parse HTTP inputs and translate service errors only. | When a route family needs counts, scoped queries, or multi-step read/write assembly, put that workflow in the owning service module and keep the router at transport concerns. |
| `review::.::holistic::abstraction_fitness::message_history_rewraps_attachment_content_api::9632fac9` | `message_history.py` reintroduced wrapper functions around vision/PDF helpers that already belonged to `attachment_content.py`, so tests and callers were depending on the wrapper surface instead of the owning module. | Switched `backend/services/agent/message_history.py` to call the `attachment_content` module directly for vision checks, PDF extraction, page rendering, and attachment assembly, promoted `normalize_pdf_text_lines` in `attachment_content.py` as the canonical normalization helper, and updated attachment-related tests to patch the owner module rather than the wrapper layer. | Keep attachment parsing and assembly helpers owned by `attachment_content.py`; callers should patch or import the owner module directly instead of rebuilding thin wrapper APIs around it. |
| `review::.::holistic::abstraction_fitness::proposal_contracts_split_across_alias_layers::d9597e74`, `review::.::holistic::ai_generated_debt::copy_pasted_nested_object_preparsers::b088db68`, `review::.::holistic::ai_generated_debt::duplicated_agent_validator_wrappers::6725c422` | Agent tool inputs mixed unrelated read/thread/admin schemas in one flat module while proposal CRUD tools depended on alias exports and duplicated membership-payload normalization outside the canonical proposal contracts. | Replaced `backend/services/agent/tool_args.py` with a focused `backend/services/agent/tool_args/` package, moved proposal CRUD tool schemas to import directly from `backend/services/agent/change_contracts.py`, and delegated group-membership wrapper validation back to `CreateGroupMemberPayload` / `DeleteGroupMemberPayload` via a shared nested-reference normalizer. | Keep canonical proposal payload ownership in one contract module, and split tool-input packages by responsibility instead of recreating parallel validator and alias layers. |
| `review::.::holistic::abstraction_fitness::run_event_payload_wrapper_chain::6a16329f` | Streaming adapters emitted run-event payloads through a pass-through wrapper in `runtime_state.py`, blurring persistence helpers with API serialization. | Deleted the `emit_run_event(...)` wrapper from `backend/services/agent/runtime_state.py` and had `backend/services/agent/runtime.py` call `stream_run_event_to_payload(...)` directly from `backend/services/agent/serializers.py`. | Keep runtime-state modules focused on persistence and state transitions; event payload shaping should stay with serializers or the runtime adapter that emits it. |
| `review::.::holistic::api_surface_coherence::agent_entry_reference_not_exclusive::5d5a3b2b`, `review::.::holistic::api_surface_coherence::find_entries_by_id_hides_prefix_search::e875522d` | Entry mutation flows silently preferred `entry_id` over `selector`, and the shared entry lookup helper hid short-id prefix behavior behind an exact-looking name. | Split entry lookup into explicit `find_entries_by_exact_id(...)` and `find_entries_by_public_id_prefix(...)` helpers, made proposal handlers validate `entry_id` + `selector` combinations instead of silently preferring one, and added regression coverage for conflicting references plus exact-vs-prefix lookup semantics. | Keep exact and prefix lookup modes explicit in helper names, and reject competing reference inputs when they do not identify the same entry candidates. |
| `review::.::holistic::api_surface_coherence::entry_commands_dual_reference_modes::e3cfad7f` | Entry service commands exposed each relationship twice (`*_id` and name fields), so callers could mix competing shapes all the way down at the service boundary. | Replaced the service-layer relationship fields on `EntryCreateCommand` / `EntryUpdateCommand` with typed `EntityRef` and `UserRef` objects in `backend/services/entries.py`, and moved the flat HTTP-payload-to-ref translation into `backend/routers/entries.py`. | Keep the service boundary on one reference shape per relationship; transport-layer payload quirks should be translated into typed refs before orchestration starts. |
| `review::.::holistic::contract_coherence::get_current_principal_provisions_state::c20f9eba` | The protected-route principal dependency looked like a pure getter even though it provisions the current user record as part of the request boundary. | Renamed `get_current_principal(...)` to `get_or_create_current_principal(...)` in `backend/auth.py`, updated protected router wiring and admin dependencies to use the honest name, and refreshed the route-boundary regression test in `backend/tests/test_auth_boundaries.py`. | Dependency names should reflect side effects at the boundary; if a dependency provisions state, make that explicit in the contract name. |
| `review::.::holistic::api_surface_coherence::group_membership_mutation_is_overloaded::f6642803` | Group membership proposals multiplexed entry targets and child-group targets through parallel nullable fields, so validation, canonicalization, review, and apply logic had to branch over an overloaded contract everywhere. | Replaced the `entry_ref` / `child_group_ref` pair with one discriminated `target` object in `backend/services/agent/change_contracts.py`, threaded that typed target through proposal args, normalization, review/apply handlers, read formatting, tool docs, and agent regression tests, and kept canonical short-id/proposal-id resolution on the new target path. | When one workflow supports multiple target kinds, represent that choice as a typed target object instead of parallel nullable fields so the contract reads directly and downstream code can share one path. |
| `review::.::holistic::contract_coherence::read_only_tools_bootstrap_users::04473c54` | Read-only agent tools were materializing `User` rows by calling `ensure_current_user(...)` just to scope account and group listings. | Extended `ToolContext` with a resolved principal snapshot, had runtime and benchmark adapters populate it with pure user lookups, switched `list_accounts` and `list_groups` to lookup-only scoping, and added direct read-handler tests proving those tools no longer create users on read paths. | Read-only tools should consume resolved identity context or pure lookups only; user provisioning belongs to explicit bootstrap or write flows, not list operations. |
| `review::.::holistic::contract_coherence::list_entities_description_disagrees_with_results::71a725fa` | `list_entities` described itself as a non-account lookup tool but still returned account-backed roots, leaving the agent with two overlapping discovery surfaces. | Filtered `list_entities` down to non-account entities in `backend/services/agent/tool_handlers_read.py`, updated the tool description/docs to state that account roots are intentionally excluded, and refreshed the agent regression coverage so account discovery stays on `list_accounts`. | Keep account and non-account discovery on separate tool contracts; if accounts have a dedicated read surface, generic entity lookups should not quietly leak them back in. |
| `review::.::holistic::contract_coherence::run_surface_serialization_is_internally_inconsistent::f91c6118` | Run reads could format `terminal_assistant_reply` for an override surface while still reporting only the persisted run `surface`, so the payload hid which surface actually shaped the reply text. | Added `reply_surface` to `AgentRunRead`, set it from the effective formatting surface in `backend/services/agent/serializers.py`, and updated run-surface docs plus regression coverage so overridden Telegram formatting is explicit in the API contract. | If a read API supports presentation overrides, expose both the persisted state and the effective presentation state instead of overloading one field to imply both. |
| `review::.::holistic::authorization_consistency::implicit_header_identity::fcae0076` | Protected HTTP routes accepted an optional principal header and silently fell back to an admin-like default identity, which blurred request auth, runtime settings, and role checks. | Replaced `backend/auth.py` with a focused `backend/auth/` package plus `backend/services/principals.py`, required explicit `X-Bill-Helper-Principal` on protected routes, stored admin access on `users.is_admin`, switched admin checks to `RequestPrincipal.is_admin`, added default authenticated test-client coverage with anonymous-route regression tests, and added a frontend `features/session/` layer so the browser owns principal selection and header injection centrally. | Authentication boundaries need one explicit source of request identity, one persisted role gate, and one frontend session owner; hidden server-side fallbacks make both security and architecture drift. |
| `review::.::holistic::contract_coherence::tool_argument_decoding_swallows_parse_failures::ebdabf26` | Agent tool-call decoding collapsed malformed JSON arguments into `{}`, so persisted tool rows and benchmark traces lied about what the model actually emitted and optional-argument tools could run on fabricated empty payloads. | Introduced typed decode results in `backend/services/agent/protocol_helpers.py`, preserved raw malformed argument strings during canonicalization, threaded `raw_arguments` plus `decode_error` through runtime and benchmark prepared-tool state, and converted decode failures into explicit tool-call errors with raw-argument metadata instead of silently executing with `{}`. | Protocol boundaries should preserve raw model payloads and emit explicit decode failures; never coerce malformed arguments into a valid-looking empty object. |
| `review::.::holistic::convention_outlier::router_handler_name_suffix_drift::831e995c` | Some routers still used ad hoc `_route` suffixes while sibling modules exposed plain handler names, which made the HTTP boundary conventions drift across the same package. | Renamed the remaining suffixed handlers in `backend/routers/groups.py` and `backend/routers/tags.py` to plain route names and aliased same-named service imports explicitly so the router layer stays consistent without shadowing service calls. | Router modules should expose direct handler names and use explicit import aliases when they need to call services with the same verb. |
| `review::.::holistic::cross_module_architecture::schema_to_service_inversion::727813a1` | Schema and tool-input models were importing validators from service modules, which created a bidirectional dependency between boundary contracts and orchestration code. | Added a neutral `backend/validation/` package for thread-title and runtime-settings validation, rewired schemas/tool args/services to import from that layer, deleted `backend/services/runtime_settings_normalization.py`, and added an import-convention guard that blocks `schemas_*.py` from importing `backend.services.*`. | Shared validators belong in a neutral boundary package; schemas should never depend on service modules for normalization or validation rules. |
| `review::.::holistic::cross_module_architecture::settings_hidden_in_finance_boundary::bc19085f` | Runtime settings types lived under finance-labeled model and schema modules even though they drive app/runtime behavior rather than ledger state. | Split runtime settings into `backend/models_settings.py` and `backend/schemas_settings.py`, rewired the settings router/service/tests/Alembic metadata imports to the dedicated modules, and added an import-convention guard that keeps the runtime settings contracts out of `*_finance` files. | Cross-cutting settings types should live in their own domain modules; do not hide application/runtime configuration under finance-labeled files. |
| `slice of review::.::holistic::cross_module_architecture::service_schema_entanglement::e4222cfd` | The runtime settings service still accepted and returned API schema models directly, which kept that service boundary coupled to HTTP request/response types even after the settings-domain split. | Added service-owned runtime settings patch/view contracts in `backend/services/runtime_settings_contracts.py`, rewired `backend/services/runtime_settings.py` and `benchmark/runner.py` to those types, moved schema translation back to `backend/routers/settings.py`, and added an import-convention guard that blocks `runtime_settings.py` from importing `backend.schemas_settings`. | Service modules should expose service-local command/read contracts and let routers translate them into HTTP schemas at the boundary. |
| `slice of review::.::holistic::cross_module_architecture::service_schema_entanglement::e4222cfd` | Account, entity, and tag write services still depended on HTTP request schemas, and finance name normalization helpers were leaking out of sibling service modules into routers and agent contracts. | Added `backend/services/finance_contracts.py` for service-owned account/entity/tag write commands, extracted shared finance normalization into `backend/validation/finance_names.py`, rewired routers plus `backend/services/agent/change_apply.py` to translate at the boundary, and added an import-convention guard blocking finance write services from importing HTTP write schemas. | Finance write workflows should share service-local command contracts plus neutral normalization helpers; routers and agent adapters must translate HTTP or proposal payloads before they enter the service layer. |
| `review::.::holistic::design_coherence::agent_propose_handler_hub::f02ef450` | Proposal handling for tags, entities, accounts, entries, groups, and pending-proposal edits was concentrated in one monolithic `tool_handlers_propose.py` module. | Split shared proposal/thread helpers into `backend/services/agent/proposals/common.py`, moved catalog and entry proposal flows into `backend/services/agent/proposals/catalog.py` and `backend/services/agent/proposals/entries.py`, kept `tool_handlers_propose.py` as the thinner group/pending-proposal coordinator, and extended import-convention coverage so the new `proposals/__init__.py` stays marker-only. | Proposal handlers should be organized by proposal family with shared support modules; keep the top-level coordinator thin so new change types do not bloat one hub file. |
| `follow-up on review::.::holistic::design_coherence::agent_propose_handler_hub::f02ef450` | The coordinator left behind in `tool_handlers_propose.py` still owned group CRUD, group-membership rules, payload normalization, and pending-proposal edits, so agent proposal behavior was still anchored to one flat module. | Split the remaining proposal coordinator into `backend/services/agent/proposals/groups.py`, `group_memberships.py`, `normalization.py`, and `pending.py`, rewired `tool_runtime.py` and `review.py` to import the owning modules directly, deleted `tool_handlers_propose.py`, and added an import-convention guard so proposal handlers stay grouped inside the package. | Proposal-family packages should own their concrete handlers and normalization flows directly; do not keep retired monoliths around as coordinating facades once the package split is in place. |
| `follow-up architectural cleanup for agent apply ownership` | The review/apply pipeline still routed every approved mutation through one flat `change_apply.py` module mixing shared lookup rules, catalog mutations, entry mutations, group mutations, and the dispatch table. | Split apply ownership into `backend/services/agent/apply/common.py`, `catalog.py`, `entries.py`, `groups.py`, and `dispatch.py`, rewired `review.py` to import the dispatch owner directly, deleted `change_apply.py`, and added import-convention guards so apply handlers stay grouped in the package. | Review-time domain mutation should be organized by mutation family with one small dispatch layer; do not centralize unrelated apply handlers in one module once a durable package boundary exists. |
| `follow-up architectural cleanup for review workflow ownership` | Approval dependency checks, payload-override normalization, audit-note formatting, and approve/reject/reopen transitions were still concentrated in one `review.py` module. | Split review ownership into `backend/services/agent/reviews/common.py`, `dependencies.py`, `overrides.py`, and `workflow.py`, rewired the agent router to import the workflow owner directly, deleted `review.py`, and added import-convention guards so review logic stays grouped in the package. | Review workflows should isolate record helpers, approval blockers, override normalization, and state transitions into focused modules; do not keep all review responsibilities in one flat service file. |
| `follow-up architectural cleanup for read-tool ownership` | Entry lookup ranking, proposal inspection, group detail rendering, catalog discovery, progress notes, and generic tool result formatting were still concentrated in one `tool_handlers_read.py` module. | Split read-tool ownership into `backend/services/agent/read_tools/common.py`, `catalog.py`, `entries.py`, `groups.py`, `proposals.py`, and `progress.py`, moved shared tool output helpers into `backend/services/agent/tool_results.py`, promoted selector ambiguity formatting into `backend/services/agent/entry_references.py`, rewired tool runtime/tests/proposal helpers to import the owning modules directly, deleted `tool_handlers_read.py`, and added import-convention guards so read tools stay grouped in the package. | Read-only tool families should be organized by lookup domain with one shared result-formatting module; do not centralize unrelated read handlers and generic helper functions in a single flat file. |
| `follow-up architectural cleanup for runtime loop ownership` | `runtime.py` still mixed stable model-call seams, run creation/resolution, tool-turn preparation, non-stream and stream loop adapters, plus terminal error/completion handling in one 800-line module. | Split the run-loop adapters and tool-turn orchestration into `backend/services/agent/runtime_loop.py`, kept `backend/services/agent/runtime.py` as the stable public seam for tests and external callers, and wired the new loop module through explicit runtime dependencies so monkeypatched `call_model`, `call_model_stream`, and `calculate_context_tokens` still govern execution. | Keep stable test seams on the public runtime module, but move loop adapters and execution-mode coordination into dedicated support modules once the coordinator starts mixing too many responsibilities. |
| `follow-up architectural cleanup for agent router ownership` | `backend/routers/agent.py` still owned thread list/detail queries, run reads, review HTTP translation, attachment delivery, background session wiring, and SSE helpers in one flat transport module. | Split the HTTP boundary into `backend/routers/agent_threads.py`, `agent_runs.py`, `agent_reviews.py`, `agent_attachments.py`, and `agent_support.py`, left `backend/routers/agent.py` as a thin aggregator, and added an import-convention guard so the aggregator stays include-only. | Large transport surfaces should be organized by endpoint family with shared HTTP helpers in one support module; keep the exported top-level router as an aggregator instead of letting it absorb every endpoint again. |
| `follow-up architectural cleanup for agent panel ownership` | `frontend/src/features/agent/AgentPanel.tsx` still mixed render composition with every query, optimistic stream path, bulk-send workflow, and local helper function in one 1300-line component. | Extracted pure panel helpers into `frontend/src/features/agent/panel/helpers.ts`, moved the stateful orchestration into `frontend/src/features/agent/panel/useAgentPanelController.ts`, and reduced `AgentPanel.tsx` to a render shell that composes the header, timeline, composer, thread rail, review modal, and preview dialog. | Large feature components should compose controller hooks and presentation modules; do not let render shells retain every query, optimistic state path, and helper function inline. |
| `review::.::holistic::abstraction_fitness::agent_change_contract_hub::68c21c77` | `backend/services/agent/change_contracts.py` had become a cross-layer hub for every proposal payload family, nested-object normalizer, and patch-path helper. | Replaced the flat module with a `backend/services/agent/change_contracts/` package, moved catalog, entry, and group payloads into focused modules, kept registry/patch validation in a small package root plus `patches.py`, rewired production imports to the owning family modules, and added an import-convention guard that keeps the flat file deleted. | Canonical contract registries may stay centralized, but concrete payload families and shared normalizers should live in focused modules so one file does not become the architecture anchor again. |
| `review::.::holistic::abstraction_fitness::duplicated_thread_proposal_query::a1c88103` | Proposal create/update helpers and the read-only proposal list tool each carried their own thread-scoped `AgentChangeItem` query, differing only by eager-load and ordering flags. | Extended `backend/services/agent/proposals/common.py` with one configurable `proposals_for_thread(...)` helper, moved review-action eager loading and ordering flags into that shared query, and deleted the duplicate SQL from `backend/services/agent/read_tools/proposals.py`. | Keep one canonical thread-scoped query per record family and make read-time variations explicit with small flags instead of cloning the whole statement into sibling modules. |
| `review::.::holistic::abstraction_fitness::internal_tool_args_barrel::a034bd44` | Production runtime, handler, and read-tool modules were still importing argument models through `backend/services/agent/tool_args/__init__.py`, so the package barrel remained part of the runtime dependency graph instead of a thin compatibility surface. | Rewired production imports to the owning `tool_args` submodules (`read.py`, `shared.py`, `threads.py`, `memory.py`, `proposal_admin.py`) and added an import-convention guard that blocks backend production code from depending on the barrel module. | Package barrels may exist for compatibility or tests, but production code should import the owning module directly so dependency ownership stays legible and refactors do not re-anchor on the barrel. |
| `review::.::holistic::abstraction_fitness::proposal_normalization_switchboard::48c8c386` | Proposal payload normalization had collapsed into one long change-type switchboard, so pending edits, review overrides, and proposal creation all depended on one flat branch-heavy module. | Split proposal normalization into family-owned modules for catalog, entry, and group payloads, moved shared parse/error helpers into `normalization_common.py`, kept `normalization.py` as a small registry dispatcher, and extended import-convention coverage plus docs to reflect the family-owned normalizers. | When proposal families normalize different payload shapes, keep each family’s normalization with its owning domain and use a small registry for dispatch instead of a long change-type switchboard. |
| `review::.::holistic::ai_generated_debt::contract_validator_boilerplate::205d64d7` | Finance write commands, agent change-contract models, and read-tool argument models repeated the same tiny name/category/currency validators and non-empty patch checks with only field names changed. | Added shared normalized field types plus a reusable `NonEmptyPatchModel` in `backend/validation/contract_fields.py`, rewired finance contracts, agent change contracts, and read-tool args to consume those shared validators, and deleted the repeated per-model wrapper methods. | Shared contract normalization rules should live in reusable field types or base models; do not duplicate tiny validator wrappers across every Pydantic contract. |
| `review::.::holistic::ai_generated_debt::duplicate_runtime_settings_update_models::1af2d886` | The service-owned runtime settings patch model and the HTTP `RuntimeSettingsUpdate` schema duplicated the same fields, bounds, and validators, so write-shape changes had two edit points and two opportunities for drift. | Added `backend/contracts_settings.py` as the neutral runtime-settings write contract, then made both `backend/schemas_settings.py` and `backend/services/runtime_settings_contracts.py` subclass that shared model instead of maintaining duplicate field definitions and validators. | When one write shape is shared across schema and service boundaries, promote it into a neutral contract module and let each boundary expose thin named wrappers rather than copy the full model twice. |
| `review::.::holistic::api_surface_coherence::resource_write_surface_inconsistent::9119f48e`, `review::.::holistic::api_surface_coherence::group_membership_target_overload::10d4e80e`, `review::.::holistic::contract_coherence::empty_patch_semantics_diverge::fc96c651` | Group and user mutations still exposed primitive-heavy service signatures, group membership create still depended on mutually-exclusive top-level ids, and sibling PATCH routes disagreed on whether an empty payload was valid. | Added shared `backend/contracts_groups.py` and `backend/contracts_users.py` write contracts, rewired group/user services and routers onto command/patch models, switched `/groups/{group_id}/members` to one typed `target` object, updated the frontend/test callers to the new payload, and enforced non-empty PATCH validation consistently across account/entity/tag/group/user update schemas. | Keep sibling write paths on one command/patch convention, model mutually-exclusive targets as discriminated unions instead of parallel optional ids, and validate empty PATCH payloads consistently at the HTTP boundary. |
| `review::.::holistic::ai_generated_debt::router_policy_translation_boilerplate::48b86e1a` | CRUD routers repeated identical local `PolicyViolation` -> HTTP translation blocks, and the access-scope helpers mirrored the same conversion pattern from inside the service layer. | Added one FastAPI exception handler in `backend/main.py` for `PolicyViolation`, removed the duplicated translation blocks from CRUD routers, and simplified `backend/services/access_scope.py` so scoped loaders keep returning the domain error contract directly. | Shared domain-policy errors should cross the HTTP boundary through one application-level exception path; do not repeat local translation blocks in every router or service helper. |
| `frontend follow-up architectural cleanup for review and composer ownership` | The agent frontend still concentrated review orchestration in `frontend/src/features/agent/review/AgentThreadReviewModal.tsx` and optimistic send/stream state in one large `frontend/src/features/agent/panel/useAgentPanelController.ts` hook, leaving modal layout, editor widgets, thread-runtime logic, and composer actions tangled together. | Split thread review ownership across `frontend/src/features/agent/review/useAgentThreadReviewController.ts`, `ReviewEditors.tsx`, and `modalHelpers.ts`, reduced `AgentThreadReviewModal.tsx` to the modal/card shell, and extracted optimistic send/stream/composer runtime into `frontend/src/features/agent/panel/useAgentComposerRuntime.ts` so `useAgentPanelController.ts` is back to query, mutation, and panel coordination. | Keep large frontend workspaces on the same pattern as the backend: one thin coordinator, dedicated controller hooks for durable state, and presentation modules that do not own unrelated query or mutation workflows. |
| `frontend follow-up architectural cleanup for review draft and diff ownership` | The review surface still depended on two large flat helper files that mixed shared coercion, domain-specific draft building, diff primitives, and proposal-family record shaping in one place. | Replaced `frontend/src/features/agent/review/drafts.ts` with a `drafts/` package split into shared helpers plus `entries`, `catalog`, and `memberships` ownership modules, replaced `frontend/src/features/agent/review/diff.ts` with a `diff/` package split into reusable diff primitives and domain record builders, and kept package-level `index.ts` files as the public import surfaces for the review controller and tests. | When review helpers start mixing multiple proposal families with shared primitives, split them into small family-owned modules and keep one package-level export surface instead of letting another flat helper hub form. |
| `frontend follow-up architectural cleanup for runtime settings ownership` | `frontend/src/pages/SettingsPage.tsx` still mixed runtime-settings queries, payload validation, form parsing, tab state, and every section card in one route file. | Extracted runtime-settings form parsing and update-payload validation into `frontend/src/features/settings/formState.ts`, moved query/mutation orchestration and form state into `frontend/src/features/settings/useSettingsPageModel.ts`, split the page UI into `SettingsToolbar.tsx`, `SettingsGeneralSection.tsx`, `SettingsAgentSection.tsx`, and `ResetSettingsDialog.tsx`, and reduced `SettingsPage.tsx` to a route shell that composes the settings feature modules. | Settings routes should follow the same page-model pattern as other frontend workspaces: one thin page shell, one controller hook for durable state, and section components that do not own API or payload logic. |
| `backend follow-up auth cleanup for catalog read scoping` | Entity and tag catalog GET endpoints still computed usage against every entry/account in the database, and the entity catalog exposed account-backed entity rows across principals. | Added reusable owner-column scoping in `backend/services/access_scope.py`, scoped `backend/services/entities.py` usage counters and account-backed entity visibility to the request principal, scoped `backend/services/tags.py` entry counts the same way, updated the entity/tag routers to depend on the current principal, and added regression coverage proving admin sees global counts while non-admin principals see only their own visible account/entity/tag usage. | Catalog read models must apply the same principal scope as sibling finance routes whenever they expose usage counters or account-backed resources; do not compute global counts in shared-read endpoints by default. |
| `review::.::holistic::authorization_consistency::agent_entry_scope_bypass::328518a7` | Agent entry reads and review-time apply lookup could see or mutate entries outside the approving principal's visibility because the read tool, entry reference helpers, and apply path were not reusing the normal owner-scope boundary. | Added reusable owner-scope conditions in `backend/services/access_scope.py`, applied principal-aware filters in `backend/services/agent/entry_references.py` and `backend/services/agent/read_tools/entries.py`, threaded reviewer `actor_name` through `backend/services/agent/apply/` plus `backend/services/agent/reviews/workflow.py`, and added regression tests proving non-admin reviewers cannot list or update another principal's entries. | Agent read/apply paths must reuse the same principal-aware scope and actor-identity seams as the normal ledger routes; review approval should never resolve entry targets through global lookups or mutable runtime settings identity. |
| `review::.::holistic::api_surface_coherence::tool_result_status_shape_drift::a7d7b0fc` | Structured agent tool results persisted uppercase `"OK"` / `"ERROR"` payload strings while the runtime, tool-call rows, and benchmark traces already used the lowercase `ToolExecutionStatus` enum values. | Canonicalized `ToolExecutionResult.output_json["status"]` to the enum value in `backend/services/agent/tool_types.py`, added focused regression coverage for the boundary contract, and updated backend/frontend expectations plus benchmark fixtures to consume the lowercase serialized status consistently. | Structured tool-result payloads should serialize the same status contract that the runtime uses internally; keep human-readable `OK` / `ERROR` text in `output_text`, but make `output_json.status` canonical and machine-stable. |
| `review::.::holistic::contract_coherence::agent_finance_contract_drift::7c20f194` | Agent catalog proposal contracts owned their own copies of the normalized account, entity, and tag create/update field models instead of reusing the finance contract layer that already anchored router and service writes. | Extracted shared catalog field models in `backend/services/finance_contracts.py`, rewired `backend/services/agent/change_contracts/catalog.py` to inherit from those shared bases while preserving the current agent-specific requirements, and added a regression test that locks the inheritance seam in place. | Shared catalog field validation should flow from one finance contract layer; agent proposal contracts may tighten or compose those shapes, but they should not duplicate the same normalized create/update field models again. |
| `review::.::holistic::convention_outlier::agent_tool_family_layout_drift::db535536` | Agent tool implementations mixed a `read_tools/` package with flat singleton handler modules for memory and thread operations, while `send_intermediate_update` lived under `read_tools/` even though it is a session-side effect rather than a lookup. | Created `backend/services/agent/session_tools/` for `progress.py`, `memory.py`, and `threads.py`, rewired the tool registry to import that package directly, deleted the old flat handler modules plus the misplaced `read_tools/progress.py`, and extended import-convention/docs coverage so session tools stay grouped as one family. | Keep agent tool families on durable package boundaries by behavior: lookup-only tools in `read_tools/`, proposal tools in `proposals/`, and session-mutating non-proposal tools in one dedicated package instead of scattered singleton modules. |
| `review::.::holistic::contract_coherence::tagread_populated_inconsistently::040c015c` | Entry payloads reused the same `TagRead` contract as the `/tags` catalog even though entry serializers never populated catalog-only fields like `entry_count`, so the same response model meant different things depending on endpoint. | Split `backend/schemas_finance.py` into `TagSummaryRead` for embedded entry tags and `TagRead` for `/tags` catalog responses, rewired `backend/services/serializers.py` and frontend `Entry` types to use the summary contract for `EntryRead.tags`, added a regression check in `backend/tests/test_entries.py`, and updated the API/backend docs to describe the narrower embedded tag shape. | Reuse the same read contract only when every producer can populate the same semantics; when an embedded payload is intentionally lighter than the catalog surface, give it its own named summary model. |
| `review::.::holistic::error_consistency::group_mutation_valueerror_drift` | Group services still raised raw `ValueError` for user-facing mutation failures, so `backend/routers/groups.py` owned a one-off HTTP translation path instead of reusing the app-wide CRUD policy contract. | Converted expected group mutation failures in `backend/services/groups.py` to `PolicyViolation`, let invariant reload failures stay as `RuntimeError`, removed the router-local `ValueError` translation from `backend/routers/groups.py`, and taught `backend/services/agent/tool_runtime.py` plus `backend/services/crud_policy.py` to surface `PolicyViolation` details cleanly for agent apply paths and generic error logging. | Keep user-facing mutation failures on one typed policy-error contract across routers, services, and agent apply flows; reserve plain runtime exceptions for true invariants instead of mixing them into HTTP/domain validation. |
| `review::.::holistic::api_surface_coherence::group_membership_target_overload::10d4e80e` | Group-member proposal payloads had already moved to a typed `target` union in the backend, but the contract normalizer still accepted legacy top-level member refs and the frontend review/diff stack still rebuilt the old parallel `entry_ref` / `child_group_ref` shape internally. | Dropped the legacy top-level member-ref fallback from `backend/services/agent/change_contracts/groups.py`, added a regression that requires `target`, and refactored the frontend review membership helpers (`drafts/memberships.ts`, `diff/domains.ts`, `ReviewEditors.tsx`, and their tests) to read and write the same `target` union the backend emits. | Once a union-shaped contract exists, carry it through every consumer instead of rebuilding parallel mutually-exclusive fields in one layer and compatibility-normalizing them away in another. |
| `review::.::holistic::error_consistency::agent_review_http_string_matching` | The review router still decided between `400`, `409`, and `422` by parsing `ValueError` message text from the approve/reject/reopen workflow, so review semantics depended on string suffixes and override wording instead of a typed policy contract. | Added `unprocessable_content` to `PolicyViolation`, converted review status conflicts and dependency/override validation in `backend/services/agent/reviews/` onto typed policy errors, removed the router-local string parser from `backend/routers/agent_reviews.py`, and preserved invalid override behavior by translating normalization `ValidationError`s into `422` policy violations. | HTTP status selection should live with the error contract, not in router string parsing; once review workflows know whether a failure is bad input, a conflict, or an unapprovable dependency, propagate that typed status directly through the shared exception handler. |
| `follow-up architectural cleanup for group membership proposal ownership` | Group membership proposal handling had become its own large feature hub, still mixing canonical reference resolution, rule validation, pending-conflict signatures, and proposal creation in one file. | Replaced `backend/services/agent/proposals/group_memberships.py` with a `group_memberships/` package split into `common.py` for canonical refs and preview/signature helpers, `validation.py` for add/remove rule enforcement, and `handlers.py` for payload building plus proposal/conflict entrypoints, then updated import guards and architecture docs to keep the feature grouped on that package boundary. | When one proposal family grows its own rule engine, split canonicalization, validation, and public handlers into a durable subpackage instead of letting another feature-specific monolith anchor the agent architecture. |
| `follow-up architectural cleanup for composer runtime ownership` | The frontend composer runtime still bundled stream-event hydration, optimistic timeline state, bulk launch workflows, stream send orchestration, and stop-run behavior in one large hook, which made future agent UX work depend on one broad stateful module. | Split `frontend/src/features/agent/panel/useAgentComposerRuntime.ts` into a smaller coordinator plus `useAgentComposerStreamState.ts` for stream/hydration state and `useAgentComposerActions.ts` for bulk-send, stream-send, and stop-run workflows, then updated the frontend architecture docs to keep those responsibilities explicit. | Keep agent workspace hooks on explicit execution seams: one UI coordinator, one stream-state owner, and one action workflow owner instead of another all-purpose runtime hook. |
| `follow-up architectural cleanup for review controller ownership` | The review controller still mixed catalog/settings queries, reviewer draft maps, override shaping, navigation state, and approve/reject/reopen workflows in one large hook, so future proposal families would keep extending one broad controller. | Extracted `useAgentReviewEditorResources.ts` for query-backed editor options and `useAgentReviewDraftState.ts` for reviewer draft maps plus override shaping, leaving `useAgentThreadReviewController.ts` focused on item navigation, batch state, and review actions, and updated the frontend docs to reflect the new review seams. | Keep review workspaces on explicit seams: one controller for navigation/actions, one hook for query-backed editor resources, and one hook for reviewer draft state instead of centralizing every concern in one feature hook. |
| `follow-up architectural cleanup for model client ownership` | `backend/services/agent/model_client.py` still mixed environment validation, prompt-cache configuration, usage normalization, stream delta assembly, and the retrying LiteLLM client behind one flat import path. | Kept `model_client.py` as the stable public seam, moved the grouped internals into `backend/services/agent/model_client_support/` (`environment.py`, `usage.py`, `streaming.py`, `client.py`), and added import-convention coverage plus doc updates so provider/env logic, stream reconciliation, and retrying request code stay on explicit module boundaries. | Keep the public model-client import stable for callers/tests, but move provider/env, streaming, and usage concerns into grouped support modules once the client seam starts anchoring unrelated responsibilities. |
