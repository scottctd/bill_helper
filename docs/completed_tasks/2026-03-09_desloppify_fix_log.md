# Desloppify Fix Log

Date: 2026-03-09

This file records durable fix batches completed during the March 9, 2026 desloppify cleanup pass.

| Finding ID | Problem | Concrete Fix | Prevention Rule |
|---|---|---|---|
| `review::.::holistic::abstraction_fitness::agent_runtime_wrapper_chain::87832f3e`, `review::.::holistic::ai_generated_debt::agent_execution_passthrough_layer::e3fd931a` | Agent runtime and benchmark callers depended on wrapper-only forwarding helpers instead of the owning modules. | Removed one-line passthrough helpers from `backend/services/agent/execution.py`, moved production imports to the owning runtime/tool modules, and kept `backend/services/agent/tools.py` as an explicit compatibility re-export surface. | Compatibility files may re-export stable contracts, but production code should import the owner module directly instead of layering wrapper seams. |
| `review::.::holistic::abstraction_fitness::change_pipeline_payload_bags::9f5d37fb` | The agent review/apply path repeatedly reparsed loose dict payload bags instead of sharing one typed change contract. | Added canonical typed payload parsing in `backend/services/agent/change_contracts.py` and threaded typed payload objects through `review.py`, `change_apply.py`, entry/group reference helpers, and proposal handlers. | Parse and validate change payloads once at the boundary, then pass typed objects through the rest of the pipeline. |
| `review::.::holistic::contract_coherence::stringly_typed_tool_result_status::ef3bf11d` | Tool execution results encoded status with raw strings, so runtime, handlers, and benchmark traces could drift. | Introduced `ToolExecutionStatus` in `backend/services/agent/tool_types.py` and updated runtime, handlers, compatibility exports, and benchmark traces to use the enum consistently. | Shared cross-module status/result values should come from one typed enum, not repeated string literals. |
| `review::.::holistic::convention_outlier::agent_normalization_helpers_split_across_siblings::bec181cb` | Agent payload normalization helpers were duplicated across sibling modules. | Extracted shared text/category normalization helpers into `backend/services/agent/payload_normalization.py` and updated callers to use the canonical module. | Common normalization rules belong in one shared helper module so validation semantics cannot drift across siblings. |
| `review::.::holistic::api_surface_coherence::account_update_parameter_sprawl::11505d20` | `update_account_root(...)` exposed partial-update behavior through a sentinel-heavy multi-parameter API. | Added a service-owned `AccountPatch` model in `backend/services/accounts.py`, switched router and agent update flows to build that typed patch, and added regression coverage for clearing account notes through both paths. | Partial mutations should travel through one explicit patch contract shared by every caller instead of public sentinel arguments. |
| `review::.::holistic::api_surface_coherence::service_error_contract_drift::9665492c` | Account payload services translated CRUD failures directly into HTTP exceptions while sibling finance services used a shared policy-error contract. | Switched `backend/services/accounts.py` back to `PolicyViolation` for account create/update validation and let `backend/routers/accounts.py` own the HTTP translation, while agent apply handlers normalize the same policy errors into proposal-friendly `ValueError` messages. | Service modules should expose one shared domain/policy error contract and leave HTTP status translation at router boundaries. |
| `review::.::holistic::contract_coherence::admin_policy_literal_drift::10fee856` | Admin-only behavior depended on repeated raw `"admin"` string checks spread across auth, access-scope, routers, and agent readers. | Promoted canonical admin helpers to `backend/auth.py` and updated access scoping, user routes, and agent read tools to reuse that one source of truth. | Permission rules should flow from one canonical helper or constant, not repeated string literals across modules. |
| `review::.::holistic::contract_coherence::currencies_route_owner_scope_gap::35eb587d` | `/currencies` counted every entry in the database instead of following the principal-aware owner scope used by sibling finance endpoints. | Added request-principal dependency and `entry_owner_filter(...)` scoping to `backend/routers/currencies.py`, plus a regression test that non-admin users only see their own non-default currency counts. | Finance read endpoints must apply the same owner-scope helpers as sibling routes so aggregate data does not leak across principals. |
| `review::.::holistic::contract_coherence::json_helper_allows_empty_model_payloads::e4985269` | The Telegram API client treated a successful empty response body as `None` even though every current caller immediately validates it as a concrete model payload. | Tightened `telegram/bill_helper_api.py` so `_read_json_response()` raises `BillHelperApiError` on empty or invalid JSON success bodies, and added `telegram/tests/test_bill_helper_api.py` coverage for the fail-closed behavior. | Transport helpers should expose typed success contracts that either return valid JSON payloads or raise immediately; they should not leak sentinel `None` values into model-bound callers. |
| `security::backend/services/agent/model_client.py::security::hardcoded_secret_name::backend/services/agent/model_client.py::44`, `security::telegram/webhook.py::security::hardcoded_secret_name::telegram/webhook.py::16` | Detector-facing constant names made configuration/header labels look like embedded secrets even though they only named environment and header keys. | Renamed the constants to auth/header terminology (`_BEDROCK_AUTH_ENV_NAME`, `TELEGRAM_WEBHOOK_HEADER_NAME`) while preserving the underlying env var and HTTP header values and keeping webhook/model-client behavior unchanged. | Constants that identify config keys or header names should describe their role, not imply that the constant itself stores secret material. |
| `review::.::holistic::design_coherence::entries_router_owns_domain_workflow::f2b899d0` | Entry create/update handlers still performed ownership checks, entity/user resolution, tag mutation, and direct-group persistence inside the router layer. | Added service-owned `EntryCreateCommand` and `EntryUpdateCommand` workflows in `backend/services/entries.py`, promoted principal-scoped loaders in `backend/services/access_scope.py`, slimmed `backend/routers/entries.py` back to HTTP translation, and added direct service tests that cover grouped entry creation plus policy-contract failures. | Routers should translate HTTP only; multi-step entry mutation workflows belong behind typed service commands with direct service-level regression tests. |
| `review::.::holistic::convention_outlier::legacy_backend_facades_obscure_split_modules::2ebcd0f1`, `review::.::holistic::cross_module_architecture::legacy_facades_still_shape_architecture::7bf673dc` | Thin `backend/models.py`, `backend/schemas.py`, and `backend/enums.py` facades still anchored docs and tests to the legacy aggregate surface even though production code had already moved to split domain modules. | Deleted the three facade modules, updated the remaining test import to use `backend.models_agent` and `backend.enums_agent` directly, added a guard in `backend/tests/test_import_conventions.py` to keep the facades deleted, and rewrote stable docs to reference the real `*_finance` and `*_agent` modules. | Once a domain split is complete, remove aggregate compatibility facades and point docs/tests at the owning modules so old architecture landmarks cannot re-form. |
| `review::.::holistic::cross_module_architecture::router_service_boundary_drift::a0f9a33b` | Tags, taxonomies, users, and dashboard routers still assembled persistence queries and read-model orchestration inline instead of delegating those workflows to services. | Moved tag read-model assembly into `backend/services/tags.py`, taxonomy read/write orchestration into `backend/services/taxonomy.py`, user list/update read models into `backend/services/users.py`, and dashboard read assembly into `backend/services/finance.py`, leaving the routers to parse HTTP inputs and translate service errors only. | When a route family needs counts, scoped queries, or multi-step read/write assembly, put that workflow in the owning service module and keep the router at transport concerns. |
| `review::.::holistic::abstraction_fitness::message_history_rewraps_attachment_content_api::9632fac9` | `message_history.py` reintroduced wrapper functions around vision/PDF helpers that already belonged to `attachment_content.py`, so tests and callers were depending on the wrapper surface instead of the owning module. | Switched `backend/services/agent/message_history.py` to call the `attachment_content` module directly for vision checks, PDF extraction, page rendering, and attachment assembly, promoted `normalize_pdf_text_lines` in `attachment_content.py` as the canonical normalization helper, and updated attachment-related tests to patch the owner module rather than the wrapper layer. | Keep attachment parsing and assembly helpers owned by `attachment_content.py`; callers should patch or import the owner module directly instead of rebuilding thin wrapper APIs around it. |
| `review::.::holistic::abstraction_fitness::proposal_contracts_split_across_alias_layers::d9597e74`, `review::.::holistic::ai_generated_debt::copy_pasted_nested_object_preparsers::b088db68`, `review::.::holistic::ai_generated_debt::duplicated_agent_validator_wrappers::6725c422` | Agent tool inputs mixed unrelated read/thread/admin schemas in one flat module while proposal CRUD tools depended on alias exports and duplicated membership-payload normalization outside the canonical proposal contracts. | Replaced `backend/services/agent/tool_args.py` with a focused `backend/services/agent/tool_args/` package, moved proposal CRUD tool schemas to import directly from `backend/services/agent/change_contracts.py`, and delegated group-membership wrapper validation back to `CreateGroupMemberPayload` / `DeleteGroupMemberPayload` via a shared nested-reference normalizer. | Keep canonical proposal payload ownership in one contract module, and split tool-input packages by responsibility instead of recreating parallel validator and alias layers. |
| `review::.::holistic::abstraction_fitness::run_event_payload_wrapper_chain::6a16329f` | Streaming adapters emitted run-event payloads through a pass-through wrapper in `runtime_state.py`, blurring persistence helpers with API serialization. | Deleted the `emit_run_event(...)` wrapper from `backend/services/agent/runtime_state.py` and had `backend/services/agent/runtime.py` call `stream_run_event_to_payload(...)` directly from `backend/services/agent/serializers.py`. | Keep runtime-state modules focused on persistence and state transitions; event payload shaping should stay with serializers or the runtime adapter that emits it. |
| `review::.::holistic::api_surface_coherence::agent_entry_reference_not_exclusive::5d5a3b2b`, `review::.::holistic::api_surface_coherence::find_entries_by_id_hides_prefix_search::e875522d` | Entry mutation flows silently preferred `entry_id` over `selector`, and the shared entry lookup helper hid short-id prefix behavior behind an exact-looking name. | Split entry lookup into explicit `find_entries_by_exact_id(...)` and `find_entries_by_public_id_prefix(...)` helpers, made proposal handlers validate `entry_id` + `selector` combinations instead of silently preferring one, and added regression coverage for conflicting references plus exact-vs-prefix lookup semantics. | Keep exact and prefix lookup modes explicit in helper names, and reject competing reference inputs when they do not identify the same entry candidates. |
| `review::.::holistic::api_surface_coherence::entry_commands_dual_reference_modes::e3cfad7f` | Entry service commands exposed each relationship twice (`*_id` and name fields), so callers could mix competing shapes all the way down at the service boundary. | Replaced the service-layer relationship fields on `EntryCreateCommand` / `EntryUpdateCommand` with typed `EntityRef` and `UserRef` objects in `backend/services/entries.py`, and moved the flat HTTP-payload-to-ref translation into `backend/routers/entries.py`. | Keep the service boundary on one reference shape per relationship; transport-layer payload quirks should be translated into typed refs before orchestration starts. |
| `review::.::holistic::contract_coherence::get_current_principal_provisions_state::c20f9eba` | The protected-route principal dependency looked like a pure getter even though it provisions the current user record as part of the request boundary. | Renamed `get_current_principal(...)` to `get_or_create_current_principal(...)` in `backend/auth.py`, updated protected router wiring and admin dependencies to use the honest name, and refreshed the route-boundary regression test in `backend/tests/test_auth_boundaries.py`. | Dependency names should reflect side effects at the boundary; if a dependency provisions state, make that explicit in the contract name. |
| `review::.::holistic::api_surface_coherence::group_membership_mutation_is_overloaded::f6642803` | Group membership proposals multiplexed entry targets and child-group targets through parallel nullable fields, so validation, canonicalization, review, and apply logic had to branch over an overloaded contract everywhere. | Replaced the `entry_ref` / `child_group_ref` pair with one discriminated `target` object in `backend/services/agent/change_contracts.py`, threaded that typed target through proposal args, normalization, review/apply handlers, read formatting, tool docs, and agent regression tests, and kept canonical short-id/proposal-id resolution on the new target path. | When one workflow supports multiple target kinds, represent that choice as a typed target object instead of parallel nullable fields so the contract reads directly and downstream code can share one path. |
| `review::.::holistic::contract_coherence::read_only_tools_bootstrap_users::04473c54` | Read-only agent tools were materializing `User` rows by calling `ensure_current_user(...)` just to scope account and group listings. | Extended `ToolContext` with a resolved principal snapshot, had runtime and benchmark adapters populate it with pure user lookups, switched `list_accounts` and `list_groups` to lookup-only scoping, and added direct read-handler tests proving those tools no longer create users on read paths. | Read-only tools should consume resolved identity context or pure lookups only; user provisioning belongs to explicit bootstrap or write flows, not list operations. |
| `review::.::holistic::contract_coherence::list_entities_description_disagrees_with_results::71a725fa` | `list_entities` described itself as a non-account lookup tool but still returned account-backed roots, leaving the agent with two overlapping discovery surfaces. | Filtered `list_entities` down to non-account entities in `backend/services/agent/tool_handlers_read.py`, updated the tool description/docs to state that account roots are intentionally excluded, and refreshed the agent regression coverage so account discovery stays on `list_accounts`. | Keep account and non-account discovery on separate tool contracts; if accounts have a dedicated read surface, generic entity lookups should not quietly leak them back in. |
| `review::.::holistic::contract_coherence::run_surface_serialization_is_internally_inconsistent::f91c6118` | Run reads could format `terminal_assistant_reply` for an override surface while still reporting only the persisted run `surface`, so the payload hid which surface actually shaped the reply text. | Added `reply_surface` to `AgentRunRead`, set it from the effective formatting surface in `backend/services/agent/serializers.py`, and updated run-surface docs plus regression coverage so overridden Telegram formatting is explicit in the API contract. | If a read API supports presentation overrides, expose both the persisted state and the effective presentation state instead of overloading one field to imply both. |
| `review::.::holistic::authorization_consistency::implicit_header_identity::fcae0076` | Protected HTTP routes accepted an optional principal header and silently fell back to an admin-like default identity, which blurred request auth, runtime settings, and role checks. | Replaced `backend/auth.py` with a focused `backend/auth/` package plus `backend/services/principals.py`, required explicit `X-Bill-Helper-Principal` on protected routes, stored admin access on `users.is_admin`, switched admin checks to `RequestPrincipal.is_admin`, added default authenticated test-client coverage with anonymous-route regression tests, and added a frontend `features/session/` layer so the browser owns principal selection and header injection centrally. | Authentication boundaries need one explicit source of request identity, one persisted role gate, and one frontend session owner; hidden server-side fallbacks make both security and architecture drift. |
| `review::.::holistic::contract_coherence::tool_argument_decoding_swallows_parse_failures::ebdabf26` | Agent tool-call decoding collapsed malformed JSON arguments into `{}`, so persisted tool rows and benchmark traces lied about what the model actually emitted and optional-argument tools could run on fabricated empty payloads. | Introduced typed decode results in `backend/services/agent/protocol_helpers.py`, preserved raw malformed argument strings during canonicalization, threaded `raw_arguments` plus `decode_error` through runtime and benchmark prepared-tool state, and converted decode failures into explicit tool-call errors with raw-argument metadata instead of silently executing with `{}`. | Protocol boundaries should preserve raw model payloads and emit explicit decode failures; never coerce malformed arguments into a valid-looking empty object. |
| `review::.::holistic::convention_outlier::router_handler_name_suffix_drift::831e995c` | Some routers still used ad hoc `_route` suffixes while sibling modules exposed plain handler names, which made the HTTP boundary conventions drift across the same package. | Renamed the remaining suffixed handlers in `backend/routers/groups.py` and `backend/routers/tags.py` to plain route names and aliased same-named service imports explicitly so the router layer stays consistent without shadowing service calls. | Router modules should expose direct handler names and use explicit import aliases when they need to call services with the same verb. |
| `review::.::holistic::cross_module_architecture::schema_to_service_inversion::727813a1` | Schema and tool-input models were importing validators from service modules, which created a bidirectional dependency between boundary contracts and orchestration code. | Added a neutral `backend/validation/` package for thread-title and runtime-settings validation, rewired schemas/tool args/services to import from that layer, deleted `backend/services/runtime_settings_normalization.py`, and added an import-convention guard that blocks `schemas_*.py` from importing `backend.services.*`. | Shared validators belong in a neutral boundary package; schemas should never depend on service modules for normalization or validation rules. |
| `review::.::holistic::cross_module_architecture::settings_hidden_in_finance_boundary::bc19085f` | Runtime settings types lived under finance-labeled model and schema modules even though they drive app/runtime behavior rather than ledger state. | Split runtime settings into `backend/models_settings.py` and `backend/schemas_settings.py`, rewired the settings router/service/tests/Alembic metadata imports to the dedicated modules, and added an import-convention guard that keeps the runtime settings contracts out of `*_finance` files. | Cross-cutting settings types should live in their own domain modules; do not hide application/runtime configuration under finance-labeled files. |
| `slice of review::.::holistic::cross_module_architecture::service_schema_entanglement::e4222cfd` | The runtime settings service still accepted and returned API schema models directly, which kept that service boundary coupled to HTTP request/response types even after the settings-domain split. | Added service-owned runtime settings patch/view contracts in `backend/services/runtime_settings_contracts.py`, rewired `backend/services/runtime_settings.py` and `benchmark/runner.py` to those types, moved schema translation back to `backend/routers/settings.py`, and added an import-convention guard that blocks `runtime_settings.py` from importing `backend.schemas_settings`. | Service modules should expose service-local command/read contracts and let routers translate them into HTTP schemas at the boundary. |
| `slice of review::.::holistic::cross_module_architecture::service_schema_entanglement::e4222cfd` | Account, entity, and tag write services still depended on HTTP request schemas, and finance name normalization helpers were leaking out of sibling service modules into routers and agent contracts. | Added `backend/services/finance_contracts.py` for service-owned account/entity/tag write commands, extracted shared finance normalization into `backend/validation/finance_names.py`, rewired routers plus `backend/services/agent/change_apply.py` to translate at the boundary, and added an import-convention guard blocking finance write services from importing HTTP write schemas. | Finance write workflows should share service-local command contracts plus neutral normalization helpers; routers and agent adapters must translate HTTP or proposal payloads before they enter the service layer. |
| `review::.::holistic::design_coherence::agent_propose_handler_hub::f02ef450` | Proposal handling for tags, entities, accounts, entries, groups, and pending-proposal edits was concentrated in one monolithic `tool_handlers_propose.py` module. | Split shared proposal/thread helpers into `backend/services/agent/proposals/common.py`, moved catalog and entry proposal flows into `backend/services/agent/proposals/catalog.py` and `backend/services/agent/proposals/entries.py`, kept `tool_handlers_propose.py` as the thinner group/pending-proposal coordinator, and extended import-convention coverage so the new `proposals/__init__.py` stays marker-only. | Proposal handlers should be organized by proposal family with shared support modules; keep the top-level coordinator thin so new change types do not bloat one hub file. |
| `follow-up on review::.::holistic::design_coherence::agent_propose_handler_hub::f02ef450` | The coordinator left behind in `tool_handlers_propose.py` still owned group CRUD, group-membership rules, payload normalization, and pending-proposal edits, so agent proposal behavior was still anchored to one flat module. | Split the remaining proposal coordinator into `backend/services/agent/proposals/groups.py`, `group_memberships.py`, `normalization.py`, and `pending.py`, rewired `tool_runtime.py` and `review.py` to import the owning modules directly, deleted `tool_handlers_propose.py`, and added an import-convention guard so proposal handlers stay grouped inside the package. | Proposal-family packages should own their concrete handlers and normalization flows directly; do not keep retired monoliths around as coordinating facades once the package split is in place. |
| `follow-up architectural cleanup for agent apply ownership` | The review/apply pipeline still routed every approved mutation through one flat `change_apply.py` module mixing shared lookup rules, catalog mutations, entry mutations, group mutations, and the dispatch table. | Split apply ownership into `backend/services/agent/apply/common.py`, `catalog.py`, `entries.py`, `groups.py`, and `dispatch.py`, rewired `review.py` to import the dispatch owner directly, deleted `change_apply.py`, and added import-convention guards so apply handlers stay grouped in the package. | Review-time domain mutation should be organized by mutation family with one small dispatch layer; do not centralize unrelated apply handlers in one module once a durable package boundary exists. |
