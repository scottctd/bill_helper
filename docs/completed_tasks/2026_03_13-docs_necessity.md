question: do we really need those docs and doc sync? human never reads most of them and the sync process is a waste of tokens and time

the only benefit they might potentially provide is for the agent to know where to find code more efficiently, but this is questinable as we're enforcing the LLM oriented design pricinples

we should investigate.

conclusion: after reading some docs, i think they're still relevant and useful that could provide value to the agent.