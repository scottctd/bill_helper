as the number of tools grows, we could potentially use a tool to search for tools, and provide the model with a concise directory of tools

potential promblems to investigate:
- invalidate KV cache if tools are placed on top of the context window, this could be model specific